import React from "react";

export function NavGroupLabel({ label }) {
  return (
    <div className="text-[10px] font-black uppercase tracking-[0.2em] text-base-content/40 px-4 mb-3 mt-6 first:mt-2">
      {label}
    </div>
  );
}
